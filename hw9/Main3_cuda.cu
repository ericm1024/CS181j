// -*- C++ -*-
#include <cstdlib>
#include <cstdio>
#include <cstring>
#include <cmath>
#include <cassert>
#include <ctime>

#include <cuda_runtime.h>

#include "../GpuUtilities.h"

#include "Main3_cuda.cuh"

// TODO: you'll probably need to change this function call
void
calculateWeirdFunction_cuda(const unsigned int maxNumberOfBlocks,
                            const unsigned int numberOfThreadsPerBlock) {

  // TODO

  // allocate device-side inputs

  // copy host inputs to device

  // allocate device-side outputs

  // calculate the number of blocks

  // run kernel

  // copy device outputs back to host

  // free memory
}
